import React, {FC} from 'react';
import {Text, View} from 'react-native';

const RoundTripScreen: FC = () => {
  return (
    <View>
      <Text>RoundTrip Screen</Text>
    </View>
  );
};

export default RoundTripScreen;
