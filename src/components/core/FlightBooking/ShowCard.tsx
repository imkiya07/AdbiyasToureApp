import React, {FC} from 'react';
import {Text, View} from 'react-native';

const ShowCard: FC = () => {
  return (
    <View>
      <Text>Show Card</Text>
    </View>
  );
};

export default ShowCard;
